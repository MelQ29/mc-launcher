node_modules/
dist/
dist-tests/
release/
*.log
*.tsbuildinfo
.DS_Store
.vscode/
.idea/
.claude/
.superpowers/
launcher.config.local.json

# Modpack build artefacts — too large for git
*.zip
*.7z
*.jar
build-staging/
build_manifest.json
ui_manifest.json
manifests/*.json
!manifests/*.example.json

# Per-build UI assets — tracked in git as the source of truth for release-ui.js.
# Note: summermon/background.mp4 is ~82 MB; GitHub warns but accepts it (>100 MB
# would be hard-blocked without LFS). If video grows beyond 100 MB, switch to
# Git LFS — see https://git-lfs.com.

# Admin-only release/deploy tooling — not part of the launcher source
scripts/sftp-upload.py
push-key.py
